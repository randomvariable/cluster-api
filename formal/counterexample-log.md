# Counterexample log

This ledger records every spec violation surfaced by a model
checker, fuzz run, formal proof attempt, or modelled trace. Each
row is owned by an issue or a contributor and progresses
monotonically through the status workflow until closure.

## Schema

| Field | Required | Description |
| ----- | -------- | ----------- |
| Date | yes | UTC date the counterexample was first observed, formatted `YYYY-MM-DD`. |
| Spec | yes | Spec module, invariant, or proof obligation violated. |
| Action | yes | Spec action or transition that produced the counterexample. |
| Classification | yes | `quint`, `tlc`, `lean`, `runtime-checker`, `modelled-trace`, or `manual-review`. |
| Severity | yes | `Critical`, `Major`, `Minor`, or `Nitpick`, matching CAPI review severity vocabulary. |
| Status | yes | One of `Open`, `Reproduced-Test-Written`, `Fixed-In-Implementation`, or `Closed-Verified`. |
| Fix link | yes | GitHub issue/PR reference once triaged, or `none`. |
| Evidence | yes | Repository path or stable URL for the trace, model output, proof obligation, CI artefact, or verification rerun. |
| Notes | yes | Closure evidence, next action, or accepted rationale. |

## Status workflow

| Status | Meaning | Allowed next |
| ------ | ------- | ------------ |
| Open | Newly recorded, not yet triaged. | Reproduced-Test-Written |
| Reproduced-Test-Written | A failing regression, model run, checker, or proof case reproduces the counterexample. | Fixed-In-Implementation |
| Fixed-In-Implementation | The code, spec, or proof has been changed; independent verification has not yet completed. | Closed-Verified, Reproduced-Test-Written |
| Closed-Verified | Independent rerun confirms the counterexample no longer reproduces. | Open (only on regression) |

Rows are append-only. Closure does not delete; it advances the
status and adds notes.

## Maintenance rules

1. Add a row when a checker, model-fuzz run, formal trace
   validator, model checker, or proof attempt produces a new
   violation.
2. Use one row per independent counterexample. If a single run
   surfaces multiple unrelated failures, split them.
3. Keep `Fix link` as a GitHub issue or PR reference once triage
   creates one.
4. Move `Status` monotonically through the allowed values; never
   delete historical rows after closure.

## Counterexamples

| Date       | Spec                          | Action                | Classification | Severity | Status | Fix link | Evidence                                                       | Notes |
| ---------- | ----------------------------- | --------------------- | -------------- | -------- | ------ | -------- | -------------------------------------------------------------- | ----- |
| (modelling pass) | Composition.InformativenessObligation | MachineHealthCheck::DeriveCondition | modelled-trace | Major | Open | none | Reported as `example-cluster` MHC events; the v1beta1 condition on the leader's Machine carries `failed to get etcdStatus for workload cluster ...: failed to get etcd status: context deadline exceeded`, while the v1beta2 condition on the same Machine drops the diagnostic key the v1beta1 message carries.
| 2026-05-08 | (mutation testing across 9 specs) | (60 surviving mutations) | mutation-tester | Minor | Open | randomvariable/cluster-api#4 | 696 mutations attempted across 9 specs; 131 killed; 60 survived. Breakdown: 35 ForallToExists (random-walk coverage; Apalache closes most), 11 AndToOr (bound-induced conjunct redundancy), 8 DropImpliesLhs (precondition rarely activated), 3 NegateRhs, 2 ExistsToForall, 1 FlipLeToLt. See `formal/mutation-findings.md`. | Cohort tracked here; per-survivor analysis in mutation-findings.md. Run `make mutation-test` to reproduce. Survivor .qnt files in `/tmp/quint-mutations/`. |
| 2026-05-08 | Lifecycle.ConvergenceFair16 | (multiple — see notes) | tlc | Minor | Open | randomvariable/cluster-api#5 | Depth bump 8 → 12 surfaced two fairness-scope gaps in `Lifecycle.qnt`: (1) ConvergenceFair16 lassos via `EtcdCompactionStart` (the action sets etcdCompactionInProgress=true; no fair-scheduled `EtcdCompactionDone` to clear it). (2) Adding EtcdCompactionDone (ConvergenceFair17) lassos via `MachineHealthChange` flip-flop — the per-(m,h) fairness conjunct treats "some MachineHealthChange fires" as fair, but the lasso has m=1 oscillating Healthy/Unhealthy infinitely. Phase 11d's bisection found this already; depth 12 re-surfaces it. | Both findings are fairness-scope gaps, not safety bugs. The full 65-conjunct ConvergenceFair would close them but tableaus to 0 branches at 16GB heap. Closing properly requires per-(m,h) fairness conjuncts. Documented as known-limitation; depth-8 verdict still stands. |
| 2026-05-08 | ClusterE2E.EtcdJoinedRequiresKcpInit (LLM candidate) | KcpMarkInitialized | quint | Minor | Closed-Verified | randomvariable/cluster-api#10 | LLM-discovered candidate `EtcdJoinedRequiresKcpInit` (∀m: machinePhase=MachineJoinedEtcd ⇒ kcpInitialized) failed at 200×60 random walk on `formal/specs/ClusterE2E.qnt`. Counterexample: the first CP enters MachineJoinedEtcd via `CpMachineJoinsEtcd` (line ~558), but the `kcpInitialized` flip happens in a separate `KcpMarkInitialized` transition (line 666) gated on `cpMachinesReady ≥ 1`. The model permits a transient where the first CP is JoinedEtcd-but-not-yet-Ready and KCP has not yet observed the trigger. | Candidate dropped — neither a spec bug nor a model gap, just an under-stated ordering. Documented in `formal/llm-invariants.md` § ClusterE2E.qnt. The 5 surviving candidates were promoted into AllSafetyInvariants. |
| 2026-05-08 | EtcdMembership.VoterSetNonEmpty (random replay) | RemoveMember | runtime-checker | Minor | Open | randomvariable/cluster-api#11 | Trace-replay sweep at 100 traces × 15 steps surfaced ~10–15% of random EtcdMembership.qnt traces tripping `VoterSetNonEmpty` in the Go checker. Cause: the spec's `RemoveMember` permits the resulting member set to consist entirely of learners (transient during multi-step membership change), but the checker assumes a non-zero voter count throughout. The Go checker reconstructs less state than the spec — this is a checker-incompleteness category, not a Go-vs-spec deviation in upstream code. | Documented in `formal/trace-replay.md` § What the sweep has found. Long-term fix: make every checker a faithful replay of the spec's transition relation. Out of scope for issue #11; logged as known limitation. The trace-replay-sweep `--strict` flag promotes findings to CI failures for ad-hoc deep-dive runs. |
| 2026-05-08 | KCPReconcile.RemediationOnlyWhenSafe (random replay) | CompleteRemediation | runtime-checker | Minor | Open | randomvariable/cluster-api#11 | Trace-replay sweep at 100 traces × 15 steps surfaced ~50–60% of random KCPReconcile.qnt traces tripping `RemediationOnlyWhenSafe`. Cause: the checker recomputes `safe := matchableEqMachines() && quorumPreservedAfterDelete(mid)` from its own reconstructed state, but the spec's safety verdict persists in `decision[m]` and can be re-evaluated by repeated `EvaluateCanSafelyRemediate` calls. The two diverge on multi-step traces. | Same checker-incompleteness category as the EtcdMembership row above. Logged as known limitation. |
| 2026-05-09 | MachineDeploymentRollout.AvailabilityBound | OperatorChangesReplicas | quint | Minor | Open | randomvariable/cluster-api#14 | `formal/specs/MachineDeploymentRollout.qnt` random walk with `--max-samples=200 --max-steps=30` reaches a state where the operator increases `desiredReplicas` while an MHC-owned drain is already in flight. The stronger snapshot-style claim `rolloutPhase == RolloutRunning => healthyCount >= desiredReplicas - maxUnavailable` fails before replacement capacity catches up. | Kept as a deliberate counterexample candidate rather than a passing invariant. The stable battery excludes it; see `formal/verify-runbook.md` issue-14 section. |
| 2026-05-09 | MachineDeploymentRollout.NoStarveOldMS | OperatorChangesTemplate | quint | Minor | Open | randomvariable/cluster-api#14 | `formal/specs/MachineDeploymentRollout.qnt` random walk reaches a state where an old-MS scale-down decision remains observable after the operator changes the rollout template generation mid-flight. The stronger state invariant `scaleDownSelected => readyNewCount > 0` is too strong across abort / retry boundaries even though the admission guard was respected at selection time. | Kept as the second documented counterexample candidate for issue #14. The passing safety battery verifies only the invariants that survive those transient abort states. |
| 2026-05-09 | ClusterClassPatches.mergeDeterministicAllOrders | ClusterClassPatchOrderChange | quint | Minor | Open | randomvariable/cluster-api#15 | `formal/specs/ClusterClassPatches.qnt` random walk with `--step=randomStep --max-samples=200 --max-steps=20` reaches `ConflictingPatchOnSameField` followed by `ClusterClassPatchOrderChange`. In canonical order the overlapping image-field writes end at `ImageTag=0`; in reordered form they end at `ImageTag=2`. The stronger obligation that *all* patch orders yield the same merged template therefore fails immediately. | Kept as the deliberate non-confluent-merge counterexample candidate for issue #15. The stable invariant `mergeDeterministic` only asserts determinism when overlapping writes are absent. |
| 2026-05-09 | ClusterClassPatches.immutableMergedCandidate | ClusterClassImmutableFieldChanges | quint | Minor | Open | randomvariable/cluster-api#15 | `formal/specs/ClusterClassPatches.qnt` random walk with `--step=randomStep --max-samples=200 --max-steps=20` reaches an applied template with `ControlPlaneClass=1`, then executes `ClusterClassImmutableFieldChanges(4)`. The candidate merged template flips `ControlPlaneClass` to `4`, but the webhook verdict simultaneously becomes `ValidationReject`, so the applied template remains unchanged. The stronger pre-validation snapshot claim `immutableMergedCandidate` therefore fails. | Logged as an accepted counterexample candidate rather than a bug. The stable invariant `immutablePreserved` is about the applied template that survives validation, not the rejected intermediate merge candidate. |
| 2026-05-10 | Finalizers.ownerDeletionWaitsForChildrenCandidate | OwnerDeletedMachineSet | quint | Minor | Open | randomvariable/cluster-api#18 | `formal/specs/Finalizers.qnt` run with `--init=orphanedChildOrderingRun --step=OwnerDeletedMachineSet --max-steps=1` reaches a state where `MachineSetObj` disappears while `MachineObj` is still alive and `InfraMachineObj` / `BootstrapConfigObj` finalizers remain pending beneath it. | Kept as the deliberate ordering counterexample candidate for issue #18. The stable invariant set keeps `noOrphan` scoped to "owner alive OR owner also being deleted" and logs the stronger disappearance-before-children-clear claim separately. |
| 2026-05-10 | Finalizers.progressFromAnyStateCandidate | Stutter | quint | Minor | Open | randomvariable/cluster-api#18 | `formal/specs/Finalizers.qnt` run with `--init=blockedDeletionRun --step=Stutter --max-steps=0` starts in a deleting state where no controller acknowledgement has happened yet and both external stall flags are asserted (`infraQuotaExceeded=true`, `nodeDrainBlocked=true`). No `RemoveFinalizer` action is enabled, so the stronger obligation that some finalizer-removal step is immediately available from every deleting state fails. | Logged as the second issue-18 counterexample candidate. The Lean companion theorem discharges `finalizer_set_eventually_empty` under the well-founded clearing measure rather than this stronger per-state enablement claim. |
| 2026-05-10 | ConversionWebhook.roundTripStatusInformative | Init(statusProjectionLossRun) | quint | Minor | Open | randomvariable/cluster-api#19 | `formal/specs/ConversionWebhook.qnt` run with `--init=statusProjectionLossRun --step=step --invariant=roundTripStatusInformative --max-steps=0` starts in a cross-version read state where the v1beta1 surface still carries the legacy diagnostic tag while the v1beta2 projection drops it. The informativeness obligation fails immediately. | Logged as the primary issue-19 counterexample candidate and mirrored by the explicit witness theorem in `formal/proofs/ControlPlane/Informativeness.lean`. |
| 2026-05-10 | ConversionWebhook.informationLossDocumented | Init(undocumentedFieldLossRun) | quint | Minor | Open | randomvariable/cluster-api#19 | `formal/specs/ConversionWebhook.qnt` run with `--init=undocumentedFieldLossRun --step=step --invariant=informationLossDocumented --max-steps=0` starts in a state where a one-version-only status field exists only on the v1beta1 side and the drop is not documented. | Logged as the documentation-gap counterexample candidate for issue #19. The stable passing path requires every one-version-only field loss to be explicitly recorded. |
| 2026-05-10 | ConversionWebhook.noneConverterCrossVersionCandidate | Init(outageFallbackRun) | quint | Minor | Open | randomvariable/cluster-api#19 | `formal/specs/ConversionWebhook.qnt` run with `--init=outageFallbackRun --step=step --invariant=noneConverterCrossVersionCandidate --max-steps=0` starts with `webhookAvailable=false`, `noneConverter=true`, and a requested version different from the stored version. A cross-version read would therefore be served through fallback rather than a clean conversion failure. | Logged as the outage-fallback counterexample candidate for issue #19. The stable invariant `webhookFailureSafe` allows outage handling only when the stored object remains untouched and the caller sees a clean error. |
| 2026-05-10 | KubeletPKI.ownedSecretOnlyRotates | Init(userSecretRotationRun) | quint | Minor | Open | randomvariable/cluster-api#20 | `formal/specs/KubeletPKI.qnt` checked with `quint verify --main=KubeletPKI --init=userSecretRotationRun --invariant=ownedSecretOnlyRotates --max-steps=0 --backend=apalache` starts in a state where a user-provided kubeconfig Secret has already been rotated despite lacking KCP ownership. | Logged as the primary issue-20 counterexample candidate. The stable helper path rotates only Secrets controlled by `KubeadmControlPlane`. |
| 2026-05-10 | KubeletPKI.caNotRecreatedAfterInit | Init(postInitCARegenRun) | quint | Minor | Open | randomvariable/cluster-api#20 | `formal/specs/KubeletPKI.qnt` checked with `quint verify --main=KubeletPKI --init=postInitCARegenRun --invariant=caNotRecreatedAfterInit --max-steps=0 --backend=apalache` starts after KCP initialization with a fresh CA silently re-minted. | Logged as the post-init CA-regeneration counterexample candidate for issue #20. The stable controller path treats missing post-init CA as an error instead of regenerating it. |
| 2026-05-10 | KubeletPKI.rotationPreservesEndpoint | Init(endpointRewriteRun) | quint | Minor | Open | randomvariable/cluster-api#20 | `formal/specs/KubeletPKI.qnt` checked with `quint verify --main=KubeletPKI --init=endpointRewriteRun --invariant=rotationPreservesEndpoint --max-steps=0 --backend=apalache` starts in a state where kubeconfig regeneration has changed the server endpoint during client-cert rotation. | Logged as the endpoint-rewrite counterexample candidate for issue #20. The stable `RegenerateSecret` path reuses the endpoint parsed from the existing kubeconfig. |
| 2026-05-10 | Pivot.crashMutualExclusionCandidate | MidPivotControllerCrash | quint | Minor | Open | randomvariable/cluster-api#17 | `formal/specs/Pivot.qnt` run with `--init=crashDualReconcileRun --step=step --max-steps=0` starts in a failed transfer state where `ClusterObj` is already live on the destination while still live on the source, and neither side is paused. The stronger crash-state obligation that no object is ever simultaneously live on both clusters outside a pause fence therefore fails immediately. | Logged as the deliberate dual-live crash counterexample candidate for issue #17. The passing invariant `mutualExclusion` only claims that such duplication is fenced in reachable transfer states, not that arbitrary crash snapshots satisfy the stronger global exclusion claim. |
| 2026-05-10 | Pivot.partialPivotOrphanCandidate | MidPivotNetworkPartition | quint | Minor | Open | randomvariable/cluster-api#17 | `formal/specs/Pivot.qnt` run with `--init=partialOrphanRun --step=step --max-steps=0` starts in a failed partial-restore state where `MachineObj` is present on the destination while its owner chain (`MachineSetObj`, `MachineDeploymentObj`, `ClusterObj`) remains only on the source. The stronger destination-owner-closure claim therefore fails immediately. | Logged as the second issue-17 counterexample candidate. The stable invariant `noOrphan` permits source+destination duplication during transfer, but rejects destination-only children whose owners never arrived there. |
| 2026-05-10 | RuntimeSDK.restartCacheReuseCandidate | InvokeHook | quint | Minor | Open | randomvariable/cluster-api#16 | `formal/specs/RuntimeSDK.qnt` run with `--init=restartCacheReplayRun --step=InvokeHook --max-steps=1` starts after a controller restart has invalidated the in-memory response cache for the current hook generation. The retry replays the transport call, moving `rawHookCallCount` from `1` to `2` while `hookEffectCount` remains `1`. | Logged as the restart-cache replay counterexample candidate for issue #16. The stable invariants assert cache invalidation and effect-level idempotence, not zero transport replays across restart. |
| 2026-05-10 | RuntimeSDK.partialFailureSingleTransportCandidate | InvokeHook | quint | Minor | Open | randomvariable/cluster-api#16 | `formal/specs/RuntimeSDK.qnt` run with `--init=partialFailureRetryRun --step=InvokeHook --max-steps=1` starts after one extension in a two-extension chain has failed after a prefix success. Recovery replays the transport call for the same generation (`rawHookCallCount` goes `1 -> 2`) while preserving effect-level idempotence (`hookEffectCount` stays `1`). | Logged as the partial-failure replay counterexample candidate for issue #16. The stable property is `partialFailureRecoverable`, which preserves prefix integrity instead of forbidding transport-level replay. |
| 2026-05-10 | CAPDLogTranslator.AddLearnerMissingNode | LoadCAPDLogs | manual-review | Minor | Open | randomvariable/cluster-api#25 | `internal/trace/testdata/capd_translator_gap_missing_node.jsonl` contains a recognised etcd-membership log message (`"Adding etcd member"`) without the `node` field required to construct the checker record. `trace.LoadCAPDLogs` fails the line as a translator gap and `trace-validator -format capdlog -differential` classifies it as `translator-gap`. | Logged as the canonical translator-gap fixture for issue #25 so the differential replay path exercises all three outcomes: passing real-trace fixtures, checker failures, and translator-side classification. |
| 2026-05-10 | ControllerRuntime.FM70_NoLegitReadyBacklog | FaultStormOverload | quint+apalache | Minor | Open | randomvariable/cluster-api#30 | `formal/specs/ControllerRuntime.qnt` overload slice leaves the distinguished legitimate key in `InReady` after repeated fault-induced retries on another key. `quint run --main=ControllerRuntime --init=faultStormOverloadRun --step=step --invariant=FM70_NoLegitReadyBacklog --max-steps=0` reproduces the scenario directly, and `quint verify --main=ControllerRuntime --init=init --step=step --invariant=FM70_NoLegitReadyBacklog --max-steps=4 --backend=apalache` reaches the same backlog shape within depth 4. | Logged as the issue-30 overload threshold counterexample candidate. The bounded companion scenario `FM69_BoundedFaultProgress` still holds, so the model captures both the safe and overloaded regimes. |
| 2026-05-10 | CrossControllerCycle.NoCyclicLivelock | LivelockCycle | quint+apalache | Minor | Open | randomvariable/cluster-api#31 | `formal/specs/CrossControllerCycle.qnt` reaches a two-controller blocked cycle in which `Md` and `Ms` are both `InFlight`, both have issued a peer enqueue, and both still wait on stale peer cache observations. `quint run --main=CrossControllerCycle --init=livelockCycleRun --step=step --invariant=NoCyclicLivelock --max-steps=0` reproduces the scenario directly, and `quint verify --main=CrossControllerCycle --init=init --step=step --invariant=NoCyclicLivelock --max-steps=4 --backend=apalache` reaches the same shape within depth 4. | Logged as the issue-31 cyclic enqueue livelock counterexample candidate. The stabilised companion run syncs both peer cache views and then completes both reconciles, so the model captures both the stuck and recovered regimes. |
| 2026-05-10 | ReflectorRelistStorm.NoDuplicateMachineLeak | DuplicateCreateRelistStorm | quint+apalache | Minor | Open | randomvariable/cluster-api#32 | `formal/specs/ReflectorRelistStorm.qnt` models a bookmark-drop / RV-too-old relist storm against the KCP machine-create path. `quint run --main=ReflectorRelistStorm --init=duplicateCreateRelistStormRun --step=step --invariant=NoDuplicateMachineLeak --max-steps=0` reproduces the duplicate-create branch directly, and `quint verify --main=ReflectorRelistStorm --init=init --step=step --invariant=NoDuplicateMachineLeak --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-32 relist-storm duplicate-create counterexample candidate. The companion `idempotentRelistStormRun` keeps the same watch/relist shape but ignores the replay, showing the intended idempotent regime. |
| 2026-05-10 | StaleEnqueueShutdown.NoNilReadAfterDelete | NilReadAfterDelete | quint+apalache | Minor | Open | randomvariable/cluster-api#33 | `formal/specs/StaleEnqueueShutdown.qnt` models a key that remains queued or in flight after the backing object has already been deleted. `quint run --main=StaleEnqueueShutdown --init=nilReadAfterDeleteRun --step=step --invariant=NoNilReadAfterDelete --max-steps=0` reproduces the nil-read / panic-class branch directly, and `quint verify --main=StaleEnqueueShutdown --init=init --step=step --invariant=NoNilReadAfterDelete --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-33 stale-enqueue counterexample candidate. The companion `deletedObjectHandledRun` and `shutdownDrainRun` preserve the safe NotFound-handling and in-flight drain regimes. |
| 2026-05-10 | WebhookOrdering.ObservedValueIsFinal | StaleValidatorRead | quint+apalache | Minor | Open | randomvariable/cluster-api#34 | `formal/specs/WebhookOrdering.qnt` models a validator observing a field before a later mutating/defaulting step changes that field. `quint run --main=WebhookOrdering --init=staleValidatorReadRun --step=step --invariant=ObservedValueIsFinal --max-steps=0` reproduces the stale-read branch directly, and `quint verify --main=WebhookOrdering --init=init --step=step --invariant=ObservedValueIsFinal --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-34 stale-validator-read counterexample candidate. The companion `convergingReinvocationRun` shows the intended replay regime where validator re-fire converges on the final value. |
| 2026-05-10 | WebhookSelfReference.UpgradeProgressDespiteWebhookGap | FailClosedDeadlock | quint+apalache | Minor | Open | randomvariable/cluster-api#35 | `formal/specs/WebhookSelfReference.qnt` models an upgrade blocked on a self-hosted webhook that is itself restarting or unavailable. `quint run --main=WebhookSelfReference --init=failClosedDeadlockRun --step=step --invariant=UpgradeProgressDespiteWebhookGap --max-steps=0` reproduces the fail-closed deadlock directly, and `quint verify --main=WebhookSelfReference --init=init --step=stepApalache --invariant=UpgradeProgressDespiteWebhookGap --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-35 self-reference deadlock counterexample candidate. The companion `safeFallbackRun` preserves progress without opening an unsafe broad ignore window, while `ignoreWindowInvalidMutationRun` records the second explicit unsafe branch. |
| 2026-05-10 | WebhookCABundleStaleness.CurrentlyTrusted | StaleTrustOutage | quint+apalache | Minor | Open | randomvariable/cluster-api#36 | `formal/specs/WebhookCABundleStaleness.qnt` models webhook serving-cert rotation, CABundle injection, and delayed apiserver trust-cache refresh as separate steps. `quint run --main=WebhookCABundleStaleness --init=staleTrustOutageRun --step=step --invariant=CurrentlyTrusted --max-steps=0` reproduces the transient x509 trust failure directly, and `quint verify --main=WebhookCABundleStaleness --init=init --step=step --invariant=CurrentlyTrusted --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-36 stale-CABundle counterexample candidate. The companion `refreshRecoveryRun` shows the intended recovery regime once the apiserver cache catches up to the injected bundle. |
| 2026-05-10 | DryRunSideEffects.DryRunHasNoSideEffects | ViolatingDryRunProbe | quint+apalache | Minor | Open | randomvariable/cluster-api#38 | `formal/specs/DryRunSideEffects.qnt` models a webhook that declares `NoneOnDryRun` but still fires an external call while handling a dry-run admission probe. `quint run --main=DryRunSideEffects --init=violatingDryRunProbeRun --step=step --invariant=DryRunHasNoSideEffects --max-steps=0` reproduces the violating branch directly, and `quint verify --main=DryRunSideEffects --init=violatingProbeInit --step=stepApalache --invariant=DryRunHasNoSideEffects --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-38 dry-run side-effects counterexample candidate. The companion `compliantDryRunProbeRun` preserves the declared contract by suppressing real side effects during the probe. |
| 2026-05-10 | AsymmetricPartition.NoSimultaneousLeaders | DualLeaderWindow | quint+apalache | Minor | Open | randomvariable/cluster-api#39 | `formal/specs/AsymmetricPartition.qnt` models directed heartbeat / ack loss where only one network direction is blocked. `quint run --main=AsymmetricPartition --init=dualLeaderWindowRun --step=step --invariant=NoSimultaneousLeaders --max-steps=0` reproduces the dual-leader branch directly, and `quint verify --main=AsymmetricPartition --init=dualLeaderInit --step=stepApalache --invariant=NoSimultaneousLeaders --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-39 asymmetric-partition counterexample candidate. The companion `recoveredDirectionalPartitionRun` heals the directed block and reaches the recovered single-leader end-state `RecoveredToSingleLeader`. |
| 2026-05-10 | SnapshotRestoreCompaction.NoLogInconsistency | RestoreCompactionRace | quint+apalache | Minor | Open | randomvariable/cluster-api#40 | `formal/specs/SnapshotRestoreCompaction.qnt` models an operator snapshot restore overlapping both a KCP-driven membership mutation and an etcd compaction generation advance. `quint run --main=SnapshotRestoreCompaction --init=compactionRaceRun --step=step --invariant=NoLogInconsistency --max-steps=0` reproduces the inconsistency branch directly, and `quint verify --main=SnapshotRestoreCompaction --init=raceInit --step=stepApalache --invariant=NoLogInconsistency --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-40 snapshot-restore / compaction-race counterexample candidate. The companion `cleanRestoreRun` preserves both the restored member set and the log basis when no concurrent compaction/reconcile race occurs. |
| 2026-05-10 | DefragQuorumLoss.NoQuorumLossUnderSingleMaintenanceFault | DefragOverlapLoss | quint+apalache | Minor | Open | randomvariable/cluster-api#41 | `formal/specs/DefragQuorumLoss.qnt` models a 3-member etcd quorum where one voter is paused for defrag and a second voter glitches during the same maintenance window. `quint run --main=DefragQuorumLoss --init=overlapLossRun --step=step --invariant=NoQuorumLossUnderSingleMaintenanceFault --max-steps=0` reproduces the quorum-loss branch directly, and `quint verify --main=DefragQuorumLoss --init=overlapInit --step=stepApalache --invariant=NoQuorumLossUnderSingleMaintenanceFault --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-41 defrag-overlap quorum-loss counterexample candidate. The companion `serialDefragRun` preserves quorum when defrag is performed one member at a time. |
| 2026-05-10 | EtcdWalFaults.NoSilentMemberLoss | SilentCorruption | quint+apalache | Minor | Open | randomvariable/cluster-api#42 | `formal/specs/EtcdWalFaults.qnt` models a member entering a corrupt WAL state or disk-full state and then crashing without any opaque-loss detection or remediation request. `quint run --main=EtcdWalFaults --init=silentCorruptionRun --step=step --invariant=NoSilentMemberLoss --max-steps=0` reproduces the silent-loss branch directly, and `quint verify --main=EtcdWalFaults --init=silentInit --step=stepApalache --invariant=NoSilentMemberLoss --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-42 silent-member-loss counterexample candidate. The companion `detectedFaultRun` shows the intended regime where opaque member loss is observed and KCP requests remediation. |
| 2026-05-10 | EtcdMembershipBatch.NoSameBatchAddRemove | SameBatchChurn | quint+apalache | Minor | Open | randomvariable/cluster-api#43 | `formal/specs/EtcdMembershipBatch.qnt` models a single batch that both promotes a joining member and removes an existing voter. `quint run --main=EtcdMembershipBatch --init=sameBatchChurnRun --step=step --invariant=NoSameBatchAddRemove --max-steps=0` reproduces the churn branch directly, and `quint verify --main=EtcdMembershipBatch --init=sameBatchInit --step=stepApalache --invariant=NoSameBatchAddRemove --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-43 add+remove same-batch counterexample candidate. The companion `serialMembershipRun` completes the join/promote first and only removes the old voter in a later batch. |
| 2026-05-10 | EtcdFiveNodeFailure.NoDoublePromotionDuringRecovery | LeaderFollowersTripleLoss | quint+apalache | Minor | Open | randomvariable/cluster-api#44 | `formal/specs/EtcdFiveNodeFailure.qnt` fixes a 5-voter cluster plus two learner candidates and models three simultaneous failures. `quint run --main=EtcdFiveNodeFailure --init=leaderFollowersLossRun --step=step --invariant=NoDoublePromotionDuringRecovery --max-steps=0` reproduces the leader+followers recovery-ordering counterexample directly, and `quint verify --main=EtcdFiveNodeFailure --init=leaderFollowersInit --step=stepApalache --invariant=NoDoublePromotionDuringRecovery --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-44 5-node / 3-failure counterexample candidate. The companion `sequentialRecoveryRun` demonstrates the intended safe regime with one learner promotion in a recovery window. |
| 2026-05-10 | RegistryPullBackoff.NoFalseBootstrapFailure | TimeoutBeforePullClears | quint+apalache | Minor | Open | randomvariable/cluster-api#46 | `formal/specs/RegistryPullBackoff.qnt` models registry throttling that forces kubelet image-pull retries with backoff until the control-plane bootstrap timeout fires. `quint run --main=RegistryPullBackoff --init=timeoutBeforePullClearsRun --step=step --invariant=NoFalseBootstrapFailure --max-steps=0` reproduces the false bootstrap-failure branch directly, and `quint verify --main=RegistryPullBackoff --init=timeoutInit --step=stepApalache --invariant=NoFalseBootstrapFailure --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-46 registry-throttle / bootstrap-timeout counterexample candidate. The companion `boundedThrottleRun` shows the intended regime where transient pull backoff clears before bootstrap is declared failed. |
| 2026-05-10 | KubeletPlegHang.RemediationAfterStableNotReady | OvereagerPlegRemediation | quint+apalache | Minor | Open | randomvariable/cluster-api#45 | `formal/specs/KubeletPlegHang.qnt` models a transient CRI slowdown that stalls PLEG long enough to flip `NodeReady` and trigger MHC before the runtime recovers. `quint run --main=KubeletPlegHang --init=overeagerRemediationRun --step=step --invariant=RemediationAfterStableNotReady --max-steps=0` reproduces the over-eager remediation branch directly, and `quint verify --main=KubeletPlegHang --init=slowCriInit --step=stepApalache --invariant=RemediationAfterStableNotReady --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-45 transient PLEG-hang counterexample candidate. The companion `transientSlowdownRun` recovers the CRI before remediation and preserves the intended safe regime. |
| 2026-05-10 | StaticPodMemPressure.CriticalStaticPodsImmuneFromEviction | MisPriorityStaticPodEviction | quint+apalache | Minor | Open | randomvariable/cluster-api#47 | `formal/specs/StaticPodMemPressure.qnt` models a node entering memory pressure while kube-apiserver has drifted to a non-critical priority class. `quint run --main=StaticPodMemPressure --init=misPriorityEvictionRun --step=step --invariant=CriticalStaticPodsImmuneFromEviction --max-steps=0` reproduces the static-pod eviction branch directly, and `quint verify --main=StaticPodMemPressure --init=misPriorityInit --step=stepApalache --invariant=CriticalStaticPodsImmuneFromEviction --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-47 mis-priority static-pod eviction counterexample candidate. The companion `correctPriorityRun` preserves the intended regime where static pods remain immune and the workload pod is evicted instead. |
| 2026-05-10 | ClusterClassTopologyRace.ConsistentCCViewPerReconcile | TornClusterClassRead | quint+apalache | Minor | Open | randomvariable/cluster-api#84 | `formal/specs/ClusterClassTopologyRace.qnt` models topology reconciliation reading a `ClusterClass` while variable-definition updates are still in flight. `quint run --main=ClusterClassTopologyRace --init=tornReadRun --step=step --invariant=ConsistentCCViewPerReconcile --max-steps=0` reproduces the torn-read branch directly, and `quint verify --main=ClusterClassTopologyRace --init=tornReadInit --step=stepApalache --invariant=ConsistentCCViewPerReconcile --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-84 torn ClusterClass view counterexample candidate. The companion `versionPinnedReadRun` preserves the intended regime where the reconcile pins a consistent ClusterClass version before resolving variables. |
| 2026-05-10 | StaticPodHashReloadRace.NoHashCollisionAcrossDistinctIntents | ManifestHashCollision | quint+apalache | Minor | Open | randomvariable/cluster-api#48 | `formal/specs/StaticPodHashReloadRace.qnt` models two distinct static-pod intents that render to the same manifest hash. `quint run --main=StaticPodHashReloadRace --init=hashCollisionRun --step=step --invariant=NoHashCollisionAcrossDistinctIntents --max-steps=0` reproduces the collision branch directly, and `quint verify --main=StaticPodHashReloadRace --init=collisionInit --step=stepApalache --invariant=NoHashCollisionAcrossDistinctIntents --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-48 manifest-hash collision counterexample candidate. The companion `eventualReloadRun` preserves distinct intent and converges kubelet reload generation. |
| 2026-05-10 | StaticPodHashReloadRace.ReloadEventuallyConverges | StaleKubeletReload | quint | Minor | Open | randomvariable/cluster-api#48 | `formal/specs/StaticPodHashReloadRace.qnt` models kubeadm rotating the desired kubelet config while kubelet never reloads that generation. `quint run --main=StaticPodHashReloadRace --init=staleReloadRun --step=step --invariant=ReloadEventuallyConverges --max-steps=0` reproduces the stale-reload branch directly, leaving `desiredConfigGen > kubeletReloadGen` and `observedIntent != intendedIdentity`. | Logged as the issue-48 reload-fence counterexample candidate. The companion `eventualReloadRun` shows the intended regime where kubelet reload catches up to the desired config generation. |
| 2026-05-10 | BootstrapCsrLag.BootstrapTimeoutCoversCsrLatency | ApprovalTimeout | quint+apalache | Minor | Open | randomvariable/cluster-api#49 | `formal/specs/BootstrapCsrLag.qnt` models a bootstrap CSR queue where approval processing lags long enough for bootstrap to time out first. `quint run --main=BootstrapCsrLag --init=approvalTimeoutRun --step=step --invariant=BootstrapTimeoutCoversCsrLatency --max-steps=0` reproduces the timeout branch directly, and `quint verify --main=BootstrapCsrLag --init=approvalTimeoutInit --step=stepApalache --invariant=BootstrapTimeoutCoversCsrLatency --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-49 CSR-approval-timeout counterexample candidate. The companion `eventualApprovalRun` shows the intended regime where approval clears before bootstrap fails. |
| 2026-05-10 | BootstrapCsrLag.NoStrandedKubelet | StrandedKubelet | quint | Minor | Open | randomvariable/cluster-api#49 | `formal/specs/BootstrapCsrLag.qnt` models kubelet bootstrap failing without a client cert and without any CSR remaining in flight. `quint run --main=BootstrapCsrLag --init=strandedKubeletRun --step=step --invariant=NoStrandedKubelet --max-steps=0` reproduces the stranded-kubelet branch directly after bootstrap has already failed. | Logged as the issue-49 stranded-kubelet counterexample candidate. The companion `eventualApprovalRun` shows the intended regime where kubelet obtains a client cert before timeout. |
| 2026-05-10 | CloudIamPermissionLoss.NoZombieMachine | ZombieMachine403 | quint+apalache | Minor | Open | randomvariable/cluster-api#60 | `formal/specs/CloudIamPermissionLoss.qnt` models provisioning beginning under valid credentials, then IAM policy revocation causing subsequent cloud calls to return 403 while the machine already exists. `quint run --main=CloudIamPermissionLoss --init=zombieMachineRun --step=step --invariant=NoZombieMachine --max-steps=0` reproduces the zombie-machine branch directly, and `quint verify --main=CloudIamPermissionLoss --init=zombieMachineInit --step=stepApalache --invariant=NoZombieMachine --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-60 IAM-revocation zombie-machine counterexample candidate. The companion `abortAfterPersistent403Run` shows the intended regime where repeated 403s force a clean abort. |
| 2026-05-10 | ConditionMessageTruncation.RootCauseSurvivesTruncation | TailDroppedTruncation | quint+apalache | Minor | Open | randomvariable/cluster-api#72 | `formal/specs/ConditionMessageTruncation.qnt` models a long condition message where the root cause sits in the tail and the stored message is truncated to 1024 bytes without a companion event. `quint run --main=ConditionMessageTruncation --init=tailDroppedRun --step=step --invariant=RootCauseSurvivesTruncation --max-steps=0` reproduces the tail-loss branch directly, and `quint verify --main=ConditionMessageTruncation --init=tailDroppedInit --step=stepApalache --invariant=RootCauseSurvivesTruncation --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-72 condition-message tail-loss counterexample candidate. The companion `tailPreservedRun` shows the intended regime where truncation preserves the root-cause-bearing tail. |
| 2026-05-10 | ServiceAccountTokenRotation.NoSilentReconcileFailure | StaleToken401 | quint+apalache | Minor | Open | randomvariable/cluster-api#73 | `formal/specs/ServiceAccountTokenRotation.qnt` models a long-running reconcile capturing token version 1, projected token rotation to version 2, and an API call returning 401 before the controller refreshes. `quint run --main=ServiceAccountTokenRotation --init=staleTokenFailureRun --step=step --invariant=NoSilentReconcileFailure --max-steps=0` reproduces the stale-token failure branch directly, and `quint verify --main=ServiceAccountTokenRotation --init=expiredTokenInit --step=stepApalache --invariant=NoSilentReconcileFailure --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-73 token-rotation auth-failure counterexample candidate. The companion `refreshAfter401Run` shows the intended regime where a 401 triggers token refresh and retry. |
| 2026-05-10 | StatusSubresourceLag.LevelTriggeredControllersTolerateLag | StaleStatusDecision | quint+apalache | Minor | Open | randomvariable/cluster-api#70 | `formal/specs/StatusSubresourceLag.qnt` models `spec.template` changing, status lagging behind on a separate subresource write, and another controller making a decision from the stale phase. `quint run --main=StatusSubresourceLag --init=staleStatusDecisionRun --step=step --invariant=LevelTriggeredControllersTolerateLag --max-steps=0` reproduces the stale-decision branch directly, and `quint verify --main=StatusSubresourceLag --init=staleStatusInit --step=stepApalache --invariant=LevelTriggeredControllersTolerateLag --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-70 status-subresource lag counterexample candidate. The companion `convergedStatusRun` shows the intended regime where status catches up and the stale phase is cleared. |
| 2026-05-10 | MtuFragmentation.EtcdSnapshotEventuallySucceeds | SilentFragmentation | quint+apalache | Minor | Open | randomvariable/cluster-api#50 | `formal/specs/MtuFragmentation.qnt` models an etcd snapshot transfer packet that remains above the effective pod-network MTU with DF set while usable ICMP fragmentation-needed feedback is lost. `quint run --main=MtuFragmentation --init=silentFragDropRun --step=step --invariant=EtcdSnapshotEventuallySucceeds --max-steps=0` reproduces the timeout branch directly, and `quint verify --main=MtuFragmentation --init=silentFragInit --step=stepApalache --invariant=EtcdSnapshotEventuallySucceeds --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-50 MTU-drift / silent-fragmentation counterexample candidate. The companion `convergedPmtuRun` shows the intended regime where PMTU discovery converges and the transfer succeeds. |
| 2026-05-10 | CniVethRace.NoContainerStartBeforeCni | ProbeLoopBeforeVeth | quint+apalache | Minor | Open | randomvariable/cluster-api#51 | `formal/specs/CniVethRace.qnt` models kubelet starting a pod container before the CNI plugin has created the veth. `quint run --main=CniVethRace --init=probeLoopBeforeVethRun --step=step --invariant=NoContainerStartBeforeCni --max-steps=0` reproduces the probe-loop branch directly, and `quint verify --main=CniVethRace --init=probeLoopInit --step=stepApalache --invariant=NoContainerStartBeforeCni --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-51 pre-veth container-start counterexample candidate. The companion `cniReadyBeforeProbeRun` shows the intended regime where CNI catches up before the probe loop forms. |
| 2026-05-10 | LoadBalancerDrain.KcpUpgradeAccountsForLbDrain | KillBeforeDrain | quint+apalache | Minor | Open | randomvariable/cluster-api#52 | `formal/specs/LoadBalancerDrain.qnt` models a load balancer target being deregistered and the apiserver being killed before drain completes. `quint run --main=LoadBalancerDrain --init=killBeforeDrainRun --step=step --invariant=KcpUpgradeAccountsForLbDrain --max-steps=0` reproduces the blackholed-client branch directly, and `quint verify --main=LoadBalancerDrain --init=killBeforeDrainInit --step=stepApalache --invariant=KcpUpgradeAccountsForLbDrain --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-52 load-balancer drain counterexample candidate. The companion `drainBeforeKillRun` preserves the intended regime where drain completes before apiserver termination. |
| 2026-05-10 | NetworkPolicyMidFlight.NoSilentControllerStall | SilentWatchSurvives | quint+apalache | Minor | Open | randomvariable/cluster-api#53 | `formal/specs/NetworkPolicyMidFlight.qnt` models a NetworkPolicy applied after a controller already holds a long-lived watch connection. `quint run --main=NetworkPolicyMidFlight --init=silentWatchSurvivesRun --step=step --invariant=NoSilentControllerStall --max-steps=0` reproduces the silent-stall branch directly, and `quint verify --main=NetworkPolicyMidFlight --init=silentWatchInit --step=stepApalache --invariant=NoSilentControllerStall --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-53 mid-flight NetworkPolicy counterexample candidate. The companion `detectedPolicyCutRun` shows the intended regime where the connection cut is detected and the controller recovers. |
| 2026-05-10 | ClusterResourceSetTiming.ApplyOnceEventuallyTakesEffect | ApplyBeforeJoin | quint+apalache | Minor | Open | randomvariable/cluster-api#85 | `formal/specs/ClusterResourceSetTiming.qnt` models `ClusterResourceSet` using `ApplyOnce` as soon as `controlPlaneInitialised=true` while worker kubelets have not yet joined. `quint run --main=ClusterResourceSetTiming --init=applyBeforeJoinRun --step=step --invariant=ApplyOnceEventuallyTakesEffect --max-steps=0` reproduces the one-shot lost-effect branch directly, and `quint verify --main=ClusterResourceSetTiming --init=applyBeforeJoinInit --step=stepApalache --invariant=ApplyOnceEventuallyTakesEffect --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-85 ApplyOnce timing counterexample candidate. The companion `joinsBeforeApplyRun` shows the intended regime where kubelets join before the one-shot apply fires. |
| 2026-05-10 | ConcurrentClusterSpecEdits.NoLostEdit | LostEdit | quint+apalache | Minor | Open | randomvariable/cluster-api#65 | `formal/specs/ConcurrentClusterSpecEdits.qnt` models operator A updating `spec.topology.version` while operator B concurrently applies a stale full-object edit to `spec.topology.controlPlane.replicas`. `quint run --main=ConcurrentClusterSpecEdits --init=lostEditRun --step=step --invariant=NoLostEdit --max-steps=0` reproduces the stale-overwrite branch directly, and `quint verify --main=ConcurrentClusterSpecEdits --init=lostEditInit --step=stepApalache --invariant=NoLostEdit --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-65 lost-edit counterexample candidate. The companion `serialEditsRun` shows the intended regime where version and replica edits both converge into the final spec. |
| 2026-05-10 | PartialRollbackDrop.RedefaultConverges | RollbackDefaultLoop | quint+apalache | Minor | Open | randomvariable/cluster-api#67 | `formal/specs/PartialRollbackDrop.qnt` models an operator backup that omits a server-defaulted field, a later `kubectl apply` that nulls it, and a controller/webhook re-default restoring it. `quint run --main=PartialRollbackDrop --init=rollbackLoopRun --step=step --invariant=RedefaultConverges --max-steps=0` reproduces the redefine-loop branch directly, and `quint verify --main=PartialRollbackDrop --init=rollbackLoopInit --step=stepApalache --invariant=RedefaultConverges --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-67 partial-rollback / dropped-defaults counterexample candidate. The companion `singleRedefaultRun` shows the intended regime where one re-default restores a stable defaulted state. |
| 2026-05-10 | ClusterEditDeleteRace.NoWriteAfterDeleteObserved | StaleWriteAfterDelete | quint+apalache | Minor | Open | randomvariable/cluster-api#68 | `formal/specs/ClusterEditDeleteRace.qnt` models `kubectl delete` landing after an edit has started but before its final persisted write. `quint run --main=ClusterEditDeleteRace --init=deleteMidEditRun --step=step --invariant=NoWriteAfterDeleteObserved --max-steps=0` reproduces the stale-write branch directly, and `quint verify --main=ClusterEditDeleteRace --init=deleteDuringEditInit --step=stepApalache --invariant=NoWriteAfterDeleteObserved --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-68 mid-edit delete counterexample candidate. The companion `editThenDeleteRun` shows the intended regime where the delete wins cleanly and no later stale write lands. |
| 2026-05-10 | ConcurrentClusterSpecEdits.NoSurgeBoundViolation | SurgeRace | quint | Minor | Open | randomvariable/cluster-api#65 | `formal/specs/ConcurrentClusterSpecEdits.qnt` models rollout surge and scale-up both increasing the control-plane population while the final desired spec is still converging. `quint run --main=ConcurrentClusterSpecEdits --init=surgeRaceRun --step=step --invariant=NoSurgeBoundViolation --max-steps=0` reproduces a temporary `actualReplicas > desiredReplicas + maxSurge` branch directly. | Logged as the issue-65 surge-race counterexample candidate. The companion `serialEditsRun` converges to the final spec without exceeding the intended bound. |
| 2026-05-10 | AutoscalerKcpSurgeRace.SurgeBoundUnderConcurrentScale | ConcurrentScaleRoll | quint+apalache | Minor | Open | randomvariable/cluster-api#87 | `formal/specs/AutoscalerKcpSurgeRace.qnt` models cluster-autoscaler scaling workers from 3 to 8 while rollout surge is still computed from a stale desired count. `quint run --main=AutoscalerKcpSurgeRace --init=concurrentScaleRollRun --step=step --invariant=SurgeBoundUnderConcurrentScale --max-steps=0` reproduces the over-replica branch directly, and `quint verify --main=AutoscalerKcpSurgeRace --init=concurrentScaleInit --step=stepApalache --invariant=SurgeBoundUnderConcurrentScale --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-87 autoscaler+rollout surge-race counterexample candidate. The companion `serializedScaleRollRun` shows the intended regime where autoscaler intent is arbitrated before rollout surge is applied. |
| 2026-05-10 | EtcdKubernetesVersionSkew.NoMidRolloutDependencyTrap | MidRolloutEtcdBump | quint+apalache | Minor | Open | randomvariable/cluster-api#81 | `formal/specs/EtcdKubernetesVersionSkew.qnt` models bumping `etcdImageTag` while a Kubernetes control-plane rollout is still mid-flight. `quint run --main=EtcdKubernetesVersionSkew --init=midRolloutEtcdBumpRun --step=step --invariant=NoMidRolloutDependencyTrap --max-steps=0` reproduces the dependency-trap branch directly, and `quint verify --main=EtcdKubernetesVersionSkew --init=dependencyTrapInit --step=stepApalache --invariant=NoMidRolloutDependencyTrap --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-81 mid-rollout etcd/Kubernetes dependency-trap counterexample candidate. The companion `orderedUpgradeRun` shows the intended regime where the control-plane rollout gate clears before the etcd tag changes. |
| 2026-05-10 | RollbackSurgeRace.NoTransientSurgeBeyondBound | RollbackMidCycleSurge | quint+apalache | Minor | Open | randomvariable/cluster-api#82 | `formal/specs/RollbackSurgeRace.qnt` models rollout surge already in flight when an operator rollback is requested and restores old replicas before the drain point. `quint run --main=RollbackSurgeRace --init=rollbackMidCycleRun --step=step --invariant=NoTransientSurgeBeyondBound --max-steps=0` reproduces the over-bound branch directly, and `quint verify --main=RollbackSurgeRace --init=rollbackMidCycleInit --step=stepApalache --invariant=NoTransientSurgeBeyondBound --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-82 rollback-mid-cycle surge counterexample candidate. The companion `serializedRollbackRun` shows the intended regime where rollback waits until the drain point before restoring old replicas. |
| 2026-05-10 | BootstrapInfraReadyRace.NoStuckUnreadyDespiteBothChildrenReady | MissingInfraReadyEvent | quint+apalache | Minor | Open | randomvariable/cluster-api#86 | `formal/specs/BootstrapInfraReadyRace.qnt` models bootstrap readiness being observed first, infrastructure readiness becoming true later, and the Machine controller never receiving the follow-up infra-ready edge. `quint run --main=BootstrapInfraReadyRace --init=missingInfraEventRun --step=step --invariant=NoStuckUnreadyDespiteBothChildrenReady --max-steps=0` reproduces the stuck-unready branch directly, and `quint verify --main=BootstrapInfraReadyRace --init=missingEventInit --step=stepApalache --invariant=NoStuckUnreadyDespiteBothChildrenReady --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-86 bootstrap/infra readiness race counterexample candidate. The companion `bothReadyObservedRun` shows the intended regime where both child-ready edges are observed and Machine ready converges correctly. |
| 2026-05-10 | MachinePoolScaleConflict.NoOscillation | SpecVsProviderOscillation | quint+apalache | Minor | Open | randomvariable/cluster-api#89 | `formal/specs/MachinePoolScaleConflict.qnt` models CAPI setting `MachinePool.spec.replicas` while the provider independently rebalances actual scale. `quint run --main=MachinePoolScaleConflict --init=oscillationRun --step=step --invariant=NoOscillation --max-steps=0` reproduces the alternating spec-vs-provider conflict directly, and `quint verify --main=MachinePoolScaleConflict --init=oscillationInit --step=stepApalache --invariant=NoOscillation --max-steps=4 --backend=apalache` reaches the same oscillation violation within depth 4. | Logged as the issue-89 MachinePool scale-conflict counterexample candidate. The companion `arbitratedScaleRun` shows the intended regime where ownership is explicitly handed to the provider and spec converges to actual size. |
| 2026-05-10 | KcpMhcDeleteRace.NoDoubleDelete | ConcurrentDelete | quint+apalache | Minor | Open | randomvariable/cluster-api#83 | `formal/specs/KcpMhcDeleteRace.qnt` models KCP selecting an old machine for replacement while MHC independently selects the same machine for remediation. `quint run --main=KcpMhcDeleteRace --init=concurrentDeleteRun --step=step --invariant=NoDoubleDelete --max-steps=0` reproduces the double-delete branch directly, and `quint verify --main=KcpMhcDeleteRace --init=doubleDeleteInit --step=stepApalache --invariant=NoDoubleDelete --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-83 concurrent double-delete counterexample candidate. The companion `authoritativeDeleteRun` shows the intended regime where only one controller owns deletion of the old machine. |
| 2026-05-10 | KcpMhcDeleteRace.NoReplacementCannibalisation | ReplacementCannibalised | quint | Minor | Open | randomvariable/cluster-api#83 | `formal/specs/KcpMhcDeleteRace.qnt` models a replacement machine being created and then immediately selected for MHC-driven deletion after landing on the same node/failure domain. `quint run --main=KcpMhcDeleteRace --init=replacementCannibalisedRun --step=step --invariant=NoReplacementCannibalisation --max-steps=0` reproduces the cannibalisation branch directly. | Logged as the issue-83 replacement-cannibalisation counterexample candidate. The companion `authoritativeDeleteRun` preserves the intended regime where the replacement is not immediately reselected. |
| 2026-05-10 | SelfHosted.not(Deadlocked) | EtcdRollRequiresHostedWrite | quint+apalache | Minor | Open | randomvariable/cluster-api#96 | `formal/specs/SelfHosted.qnt` now models the explicit etcd-roll trap inside a self-hosted upgrade. `quint run --main=SelfHosted --init=selfHostedEtcdTrapTrace --step=step --invariant='not(Deadlocked)' --max-steps=0` reproduces the deadlock directly, and `quint verify --main=SelfHosted --init=selfHostedUpgradeMidFlightInit --step=stepNoRecovery --invariant='not(Deadlocked)' --max-steps=4 --backend=apalache` reaches the same deadlock class within depth 4. | Logged as the issue-96 self-hosted etcd-roll trap counterexample candidate. The companion `selfHostedEtcdEscapeTrace` shows the intended regime where `EnableEscapeHatch` allows the upgrade to proceed without depending on the hosted etcd write path. |
| 2026-05-10 | ControllerManagerReplay.NoDoubleEffect | ReplayDoubleEffect | quint+apalache | Minor | Open | randomvariable/cluster-api#90 | `formal/specs/ControllerManagerReplay.qnt` models a leader controller-manager applying an effect, OOM-killing, and a new leader replaying the same effect after failover because only in-memory state recorded completion. `quint run --main=ControllerManagerReplay --init=doubleEffectReplayRun --step=step --invariant=NoDoubleEffect --max-steps=0` reproduces the double-effect branch directly, and `quint verify --main=ControllerManagerReplay --init=doubleEffectInit --step=stepApalache --invariant=NoDoubleEffect --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-90 replay double-effect counterexample candidate. The companion `cleanReplayRun` shows the intended regime where the new leader reconstructs completion from durable state instead of re-applying the effect. |
| 2026-05-10 | ControllerLeaderSplitBrain.NoDuplicateEffectAcrossLeaders | SplitBrainDuplicateEffect | quint+apalache | Minor | Open | randomvariable/cluster-api#94 | `formal/specs/ControllerLeaderSplitBrain.qnt` models a management-cluster lease split-brain where both controller-manager replicas think they are leader and both apply the same effect. `quint run --main=ControllerLeaderSplitBrain --init=splitBrainRun --step=step --invariant=NoDuplicateEffectAcrossLeaders --max-steps=0` reproduces the duplicate-effect branch directly, and `quint verify --main=ControllerLeaderSplitBrain --init=splitBrainInit --step=stepApalache --invariant=NoDuplicateEffectAcrossLeaders --max-steps=4 --backend=apalache` reaches the same violation within depth 4. | Logged as the issue-94 split-brain duplicate-effect counterexample candidate. The companion `leaseConvergesRun` shows the intended regime where lease convergence restores a single writer before any duplicate effect occurs. |
