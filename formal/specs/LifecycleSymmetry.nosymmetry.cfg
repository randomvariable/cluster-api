\* TLC configuration for LifecycleSymmetry.tla WITHOUT SYMMETRY.
\* Companion to LifecycleSymmetry.cfg — same constants, no symmetry,
\* used to compare the reduction factor.

CONSTANTS
    Machines      = {m1, m2, m3, m4, m5}
    MaxConcurrent = 2

CHECK_DEADLOCK FALSE

SPECIFICATION Spec

INVARIANTS
    AllSafetyInvariants
    InFlightBounded
    NoConcurrentQuorumLoss
    PhaseConsistent
